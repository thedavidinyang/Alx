# THIS INSTRUCTION IS FOR Vi

to create the file use the following command

touch filename

example: touch cutting

to write the Vi commandin to the file use the following command:

echo '{comand}' > filename

examle:

echo ':dd' > cutting